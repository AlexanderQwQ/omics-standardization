# =============================================================================
# omics_standardization — Python 依赖列表（一键安装所有依赖）
# =============================================================================
# 安装:
#   pip install -r requirements.txt
#
# ⚠️ 安装前请先完成以下系统级依赖的手动配置（详见下方说明）
# =============================================================================


# #############################################################################
# ##  系统级依赖（需手动安装，pip 无法管理）
# #############################################################################
#
# 1. R 语言环境 (rpy2 的运行时)
#    - Windows:  https://cran.r-project.org/bin/windows/base/
#    - macOS:    brew install r
#    - Linux:    sudo apt install r-base  (或通过 conda: conda install -c conda-forge r-base)
#
# 2. R Bioconductor 包（TMM / DESeq2 / VSN / Scran 归一化需要）
#    打开 R 终端，执行:
#    ```r
#    if (!require("BiocManager", quietly = TRUE)) install.packages("BiocManager")
#    BiocManager::install(c("edgeR", "DESeq2", "limma", "scran"))
#    ```
#
# 3. kallisto (FASTQ 双端定量需要，需在 PATH 中)
#    - conda:   conda install -c bioconda kallisto
#    - 手动:    https://pachterlab.github.io/kallisto/download
#
# 4. DM8 数据库 (仅 Windows 用户 + 达梦数据库存储)
#    需安装达梦数据库客户端，pip 安装 dmPython 需要 DM8 驱动头文件
# #############################################################################


# =============================================================================
# 核心依赖
# =============================================================================
anndata>=0.10
scanpy>=1.9
muon>=0.1
mudata>=0.2
numpy>=1.21
pandas>=1.5
scipy>=1.9
scikit-learn>=1.1
matplotlib>=3.7
seaborn>=0.12
pyyaml>=6.0
tqdm
h5py>=3.8
joblib
numba>=0.57
pydantic>=2.0
rich


# =============================================================================
# PyTorch + scvi-tools (ZINB-VAE 插补 + DANN 批次校正)
# =============================================================================
torch>=1.13
scvi-tools>=1.0


# =============================================================================
# R/rpy2 (TMM / DESeq2 / VSN / Scran 归一化)
# ⚠️ 需先安装 R 和 Bioconductor 包（见顶部系统级依赖说明）
# =============================================================================
rpy2>=3.5


# =============================================================================
# 流式细胞术 (.fcs 文件解析)
# =============================================================================
fcsparser>=0.2


# =============================================================================
# 质谱 (.mzML 文件解析)
# =============================================================================
pymzml>=2.5


# =============================================================================
# MAGIC 插补
# =============================================================================
magic-impute>=3.0


# =============================================================================
# NGS (.bam / .sam / FASTQ 解析)
# ⚠️ FASTQ 双端模式需要系统安装 kallisto 并在 PATH 中（见顶部说明）
# =============================================================================
pysam>=0.22


# =============================================================================
# 微生物组 (BIOM 格式解析)
# =============================================================================
biom-format>=2.1


# =============================================================================
# 混合存储 (MinIO 对象存储 + Neo4j 图数据库)
# =============================================================================
minio>=7.2
neo4j>=5.20
# dmPython>=8.0  # 取消注释前请先安装达梦 DM8 数据库客户端


# =============================================================================
# 开发与测试
# =============================================================================
pytest>=7.0
pytest-cov
pytest-mock
ruff
pre-commit


# =============================================================================
# 文档构建
# =============================================================================
sphinx>=6.0
myst-nb>=1.0
sphinx-book-theme>=1.0
nbsphinx
ipykernel
